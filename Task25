Q, write a program in C to find the
frequency of characters. 

#include<stdio.h>
int main() {
str[1000],ch; 
int count = 0;
fgets(str,sizeof(str), stdin);
scanf("%c",&ch);
for(int i=0;str[i] != '\0';i++) {
if (ch == str[i])
   ++count; 
    }
    printf("%d", count);
    return 0;
    }
