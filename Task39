Write a C program to find the factorial of a given number using a recursive function

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

 #include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

   int main()
   {
       int p;
       scanf("%d",&p);
       if (p<0){
           printf("ERROR");
            }
       else
       {
           printf("%ld",multiplynumbers(p));
             }
       return 0;
   }
int multiplynumbers(int p){
    if(p==0|p==1)
        return 1;
    else (p>=1);
    return p*multiplynumbers(p-1);
}










